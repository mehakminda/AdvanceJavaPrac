SpringCore01 ->
SpringCore02 ->
using of @Bean: developer is creating the object and asking the spring IOC container to manage it
SpringCore03 ->
SpringCore04 ->
SpringCore05 ->
SpringCore06 ->
SpringCore07 ->
SpringCore08 ->
SpringCore09 ->
SpringCore10 ->
SpringCore11 -> 


----------------------------------------------------------------------------------------------------------------------
SpringBoot01 ->
SpringBoot02 ->
SprintBoot03 ->
SpringBoot04 ->
SpringBoot05 ->
instead of qualiafier deciding which bean to inject, asking user input from user about which bean to inject. 

Same project as above but asking for user input for selecting the course name and based on that injection will happen 

----------------------------------------------------------------------------------------------------------------------
SpringJDBC01 -> Spring boot+ JDBC API +My sql driver

Problem: We still need to write the get connect, create statement objects..

SpringJDBC02 -> Spring Data JPA +h2 database
schema.sql will be executed automatically as soon as application starts
h2 is in-memory database
CommandLineRunner is executed as soon as application starts (just like static)
using JDBCTemplate(take care of all the bolier plate code of getting connect, creating statement,etc)

adding select, update, delete operation

Problem: queries are hardcoded

SpringJDBC03 ->
SpringJDBC04 ->

----------------------------------------------------------------------------------------------------------------------
Servlet:

----------------------------------------------------------------------------------------------------------------------
SpringMVC01 -> spring web+ 